# Azure DevOps Pipeline para despliegue y seeding de base de datos
# azure-pipelines.yml

trigger: none

pool: 'Laptop-Agent-Pool'

variables:
  - group: GENERAL

resources:
  repositories:
    - repository: cicd
      type: git
      name: cicd-templates
      ref: refs/heads/develop

stages:
  - template: backend/stages/ts/backend-ts-project.yml@cicd
